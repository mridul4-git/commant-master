git config --lis >-command for list config

git config --global user.name "...." >-commant for adding user name

git config --global user.email "...." >-commant for adding user email

git init >-command for initialize git repository

git status  >-commant for listing all untracked files

git adding -A >-command for adding all untracked files to staging area

git commit -m "..msg.." >-command for adding all staged files to local repository

flags
    A>- staged  
    U>-untracked
    M>-modified

command for adding git repository >- git remote add name remote_path

command for listing remote >- git remoe -v

command for adding changes in local repository to remote repository
    >-git push -u remote_name branch_name
    >-git push -u lumi master